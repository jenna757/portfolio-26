import type { ReactNode } from 'react';

/** brittanychiang.com Experience — ol.group/list */
export const ExperienceList = ({ children }: { children: ReactNode }) => (
  <ol className="group/list">{children}</ol>
);
